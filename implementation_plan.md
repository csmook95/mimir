# 구현 계획

## 1. 현재 Git 상태 확인
`git status` 명령어를 사용하여 현재 저장소의 변경 사항들을 확인합니다. 이를 통해 커밋할 파일들을 파악합니다.

## 2. 변경 내용 상세 확인
`git diff HEAD` 명령어를 사용하여 변경된 파일들의 구체적인 내용을 확인합니다. 이 단계는 커밋 메시지에 포함될 "무엇이 변경되었는지"를 이해하는 데 중요합니다.

## 3. 기존 커밋 메시지 스타일 분석
`git log -n 3` 명령어를 통해 최근 3개의 커밋 메시지를 확인하여 프로젝트의 커밋 메시지 작성 스타일(형식, 상세 수준, 서명 등)을 파악합니다.

## 4. 커밋 메시지 초안 작성
위에서 수집된 정보를 바탕으로 커밋 메시지의 초안을 작성합니다. 이 단계에서는 변경 내용의 "무엇(what)"과 "왜(why)"를 명확히 설명하는 데 중점을 둡니다.

## 5. 사용자 승인 요청
작성된 커밋 메시지 초안과 변경 내용 요약을 사용자에게 제시하고, 승인을 요청합니다. 사용자의 승인 없이는 실제 커밋 작업을 진행하지 않습니다.

## 6. (승인 시) 커밋 실행
사용자로부터 승인이 떨어지면, `git add` 명령어로 변경 사항을 스테이징하고, `git commit -m "초안 메시지"` 명령어로 커밋을 실행합니다.

## 7. (선택 사항) 커밋 확인
`git status` 명령어로 커밋이 성공적으로 완료되었는지 확인합니다.